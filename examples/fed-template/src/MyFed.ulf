

reactor Src {
  output out: int

  reaction(startup) -> out {=
  =}
}


reactor Dest {
  input in: int

  reaction(in) {=

  =}
}


federated reactor {
  @platform("native")
  src = new Src()

  @platform("zephyr")
  dest = new Dest()

  src.out -> dest.in
}