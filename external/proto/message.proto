
import "generator/proto/nanopb.proto";

message PortMessage {
  required int32 connection_number = 1; // FIXME: I think we can safely reduce to 16 or even 8 bit.
  required string message = 2 [(nanopb).max_length = 832];
}
