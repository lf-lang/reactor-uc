target uC

@timeout(1h)
@logging("ERROR")
@fast(true)
@platform("native")
main reactor {
  timer t(0, 1 sec)
  
  reaction(t) {= =}

  reaction(shutdown) {=
    printf("Finally shutdown executed for " PRINTF_TIME " logical sec\n", env->get_elapsed_logical_time(env)/SEC(1));
  =}
}
