target uC

@logging("WARN")
@timeout(100ms)
@platform("native")
main reactor {
  timer t(0, 10 msec)

  reaction(t) {=
    printf("Hello World! at %ld\n", env->get_elapsed_physical_time(env));
  =}
}
