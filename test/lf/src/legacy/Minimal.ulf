// This is a smoke test of a minimal reactor.

preamble {=
  #include "reactor-uc/reactor-c.h"
=}

@platform("native")
main reactor Minimal {
  reaction(startup) {=
    printf("Hello World.\n");
  =}
}
